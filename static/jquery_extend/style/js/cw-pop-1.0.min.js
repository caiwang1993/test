/**
 * Created by cw on 2020/4/1.
 * 弹窗
 * var pop = $(".pop").pop({
 * 默认参数
 *
 * })
 */
//js 立即执行函数
;(function (f) {
   f.fn.pop = function (g) {
       // g 为传进来的参数
       let a = this;
       console.log(a)
       a.options = f.extend(!0,{
           text:'',
           speed:'800',
           main:'.pop-box',
           flag:!0,
       },g);
       //打开弹窗

       a.open = function (b,e,c) {

           a.addClass(e).find(a.options.main).html(c);
           a.fadeIn(b)
       };
       //关闭弹窗
       a.close = function (b) {
           a.fadeOut()
           a.find(a.options.main).empty()
       };

       (function () {
           var b = a.options.main.replace(/^(#|\.)/,"");
           a.html("").append('<div class="pop-table"><div class="pop-cell"><div class="' + b + '"></div></div></div>');
       })();
       return this
   }
})(window.jQuery);